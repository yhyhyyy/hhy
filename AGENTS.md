# AGENTS.md

## Repository overview

This workspace currently contains a single architectural design document:

- `V3-企业信息化顶层架构设计-ERPCRMSRMMOM.html`

It appears to be a top-level enterprise information architecture design for a project involving ERP, CRM, SRM, and MOM systems.

## Guidance for AI agents

- Treat this repository as a documentation/design-only workspace, not a typical source-code project.
- Do not assume build, test, or runtime commands exist unless additional files are added.
- If asked to modify or generate code, first confirm whether the user wants to convert the design into a code project or create related artifacts.
- If the user requests summaries or explanations, extract key concepts from the HTML document rather than searching for code.

## Notes

- No programming language-specific files or build tooling are present at this time.
- If new files are added, update this guidance with relevant structure and conventions.
